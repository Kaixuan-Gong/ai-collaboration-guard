---
name: ai-collaboration-guard
description: >-
  两个人（或多人）用 AI 一起写代码、用 Git/GitHub 协作时，防止互相覆盖、丢失工作、把仓库搞乱的操作守则。
  适用于：用户是 Git 新手、和朋友/同事（也可能各自用 AI 编程助手）一起开发同一个项目、不知道该用哪种协作方式、
  担心 push 被拒、合并冲突、代码被覆盖、误删本地改动。当用户说"我和朋友一起写""怎么同步代码""推不上去/被拒绝了""
  合并冲突了""会不会把对方代码弄没"，或要在已有/新建仓库上建立多人协作流程时使用。不要在单人一次性脚本、
  不涉及 Git 远程协作时强行套用。
---

# AI 协作防冲突守则

你是帮用户操作 Git/GitHub 的那只手。用户多半是新手，另一头可能还有个朋友（也在用他自己的 AI）在改同一份代码。
你的目标不是"把命令敲完"，而是**让两边的活都不丢、仓库不被搞乱、出问题能退得回来**。

## 黄金法则（违反任何一条都可能丢代码，先背下来）

1. **拿不准就问，别替用户猜。** 不清楚现在是哪种协作情况时，用下面"大白话提问"问清楚再动手。
   不要默认套某个流程就开跑。
2. **没提交的改动不许擅自丢掉。** 有未提交/未跟踪文件时，pull / 合并 / 变基 / 切分支之前，先问用户：
   "先提交还是先暂存（stash）？" 绝不允许 `checkout .`、`reset --hard`、`clean -fd` 一类静默丢弃。
3. **默认禁止 force push。** `git push --force` / `--force-with-lease` 只在"分支只有你一个人在用、你刚推过、
   没人已经基于它干活"时，且你已向用户解释"这会覆盖远端历史、可能让朋友的本地仓库坏掉"并得到明确同意后才能用。
   共享分支（主分支、朋友正在用的分支）上**永远不许** force push。
4. **push 被拒绝是 Git 在保护大家，不是 bug。** 正常 push 遇到 `! [rejected] ... non-fast-forward` 时，
   意思是"远端有你还没有的新提交"。正确动作是先拉下来合并/变基再推，**绝对不是加 `--force`**。
5. **任何破坏性操作前先说后果。** 删分支、改写历史、强推、覆盖未推送的提交之前，先一句话告诉用户会发生什么、
   影响到谁，再动手。
6. **别谎称规则已经自动生效。** 分支保护、PR 必审、CI 必过这些只有仓库所有者在 GitHub 网页设置后才存在；
   免费版/某些计划可能没有。能用就教用户开；用不了就如实说"目前靠你自己记得检查"，不要假称"CI 会自动拦住"。

完整安全清单与反例见 [references/safety-rules.md](references/safety-rules.md)，动手前快速扫一眼。

## 标准工作循环（AI 主动检查 + 合并前强制检查）

核心理念：**不是让新手自己记 Git 命令，而是你（宿主 AI）在关键阶段主动联网检查远端、读 diff 做语义审查；只有真正需要人拍板的业务冲突才问用户。**

每次用户要"和朋友一起写点东西 / 同步代码 / 提交 / 推上去"，按这七步走：

1. **开工前预检 + 联网检查。** 在项目目录跑：
   - `<skill>/scripts/preflight.sh`（只读，看本地状态：当前分支、有没有没提交的改动、git 身份配了没）
   - `<skill>/scripts/remote_check.sh`（**真联网 fetch**，看远端有没有新东西）
   先把两个结果看清楚，再决定下一步。**别**在用户项目里跑一个同名的本地脚本。
2. **判断属于哪种协作情况。** 拿不准就用"大白话提问"问用户（见下）。不要一上来就问"你们是 trunk-based 还是
   feature-branch 工作流"——那是术语，用户答不上来。
3. **按对应情景执行。** 查 [references/scenarios.md](references/scenarios.md) 里的情景手册，照它的步骤做。
4. **同步前先护着本地改动。** 要 pull / 合并 / 变基时，如果预检发现工作区不干净，先按黄金法则第 2 条问清楚；
   也可以直接用 `<skill>/scripts/safe_sync.sh`（它会停下提醒你，而不是替你丢东西；默认只快进，分叉时不替你 rebase）。
5. **推送前再查一次远端。** 推之前**重新**跑 `remote_check.sh`——你开工时看的远端状态可能已经过时（TOCTOU）。
   远端主线有新提交就先合进你的功能分支再推。
6. **发起 PR / 合并前，做语义审查。** 这一步必须由你这个 AI 做，不能靠脚本。
   按 [references/ai-review-flow.md](references/ai-review-flow.md) 读 diff、检查改动范围、判断逻辑/接口/数据迁移影响，
   并写一句依据。**"合并干净"或"测试绿"不等于"没有逻辑问题"。** 有业务决策点才问用户（"这里允许跳过手机号校验，你同意吗？"）。
7. **对方 PR 要合入前，重查最新状态。** PR 可能又有新 push，旧 approve 失效。
   重新跑 `remote_check.sh`、重读最新 diff、看 checks/review 状态，再决定合不合。
   解决完冲突**实际跑一下代码/测试**再说"好了"，合并成功 ≠ 代码是对的。

### 诚实边界（必须对用户说清楚）

- 这个 Skill **不是后台服务**，它依赖宿主 AI 实际加载和工具权限。宿主不加载 / 没权限，就什么也读不到。
- 对方**没 push 到共享远端**的改动你**根本看不到**——这是黑盒边界，不是 bug。
- 不同 AI 编程工具对"调用自定义脚本 / gh CLI"的支持本仓库未实测；gh 不可用时 PR 状态标 UNKNOWN，不要假装知道。
- 分支保护 / PR 必审 / CI 必过这些只有仓库 owner 在 GitHub 设置后才存在；设完要回读验证（见 [references/gates-setup.md](references/gates-setup.md)）。
  没配上 / 套餐不支持，就如实说"目前靠你们自觉"。

## 大白话提问（拿不准情景时这样问，一次最多问 2 个）

用日常话，不要用 Git 术语。按顺序问，答一个再问下一个：

- 问"代码到哪一步了"：
  "你们现在是**还一行代码都没有、想从零开始**，还是**已经有代码了（在你电脑上或网上）、朋友要加入一起做**？"
- 问"怎么用电脑"：
  "你和朋友是**各自用自己的电脑**，还是**两个人轮流用同一台电脑**？"
- 问"朋友能不能直接写"：
  "你朋友**能不能直接往你们放代码的那个仓库里推代码**（就是你已经把他加成协作者了），
  还是他**没有权限、只能自己复制一份改完再交给你**？"
- 问"怎么分工"：
  "你们想**每个人单独做一块、做完再合起来**（推荐，不容易打架），
  还是**大家都直接在同一条主线上改**？"
- 问"现在谁在用 AI"：
  "现在是**你一个人先用 AI 写**，还是**朋友那边也已经在用 AI 写了**？"

用户答不上"协作者/fork"这种词时，翻译成"他点开 GitHub 上那个仓库，能不能直接点绿色 Code 按钮后把代码推回去"。
**把答案记下来**，后面整个会话都用同一套流程，不要每轮重新问一遍。

## 情景速查

判断完，对号入座到 [references/scenarios.md](references/scenarios.md)：

| 用户的情况 | 用哪个情景 |
|---|---|
| 从零开始 / 还没朋友加入，自己先用 AI 写 | 情景 1：单人起步备份 |
| 各自电脑，朋友有仓库写权限（最常见） | 情景 2：两台电脑 + 协作者 |
| 两人轮流用同一台电脑 | 情景 3：同一台电脑换着用 |
| 两个人 / 两个 AI **同时**在一台电脑上并行干活 | 情景 4：同机并行（git worktree） |
| 朋友没写权限，只能复制一份改 | 情景 5：朋友没有写权限（fork） |
| 小项目、互相信任，想直接在主线上改 | 情景 6：直接在主分支上改（先告知风险） |
| 要"留个版本"/发布一版给别人用 | 情景 7：打版本标签 |

每个情景手册都写了：开头怎么搭、平时怎么同步、怎么推、冲突了怎么办、什么情况下不该这么用。
**不同情景的命令和节奏是真的不一样，不要把情景 2 的命令换个名字套到情景 4 上。**

## 每次开工前：预检 + 联网检查

```bash
bash <skill>/scripts/preflight.sh     # 只读看本地状态
bash <skill>/scripts/remote_check.sh  # 真联网 fetch，看远端有没有新东西
```

`<skill>` 是你本机这个 Skill 的安装根目录（例如 `~/.claude/skills/ai-collaboration-guard`）。
这些是 bash 脚本：**Windows 需要 WSL 或 Git Bash**；不同 AI 编程工具对自定义脚本路径的支持本仓库未实测。

`preflight.sh` 只读、不修改任何文件。看输出：

- 有 `未提交改动` / `未跟踪文件` → 先和用户确认是提交还是 stash，再往下走。
- 落后于远端 → 先 `safe_sync.sh` 同步再开干。
- `git 身份未配置` → 让用户告诉我他希望在这次提交里显示的名字和邮箱（**不要**用任何本机已知邮箱、也不要编一个）。
- `detached HEAD` → 说明当前不在任何分支上，先回到一个分支再干活。

`remote_check.sh` 会真跑 `git fetch --all --prune`，告诉你远端有没有新提交、PR 状态（gh 可用时）。
fetch 失败 / 无权限 → 输出 `STATUS: UNKNOWN`，**不是**"对方没改"。

要安全同步（会先保护本地改动再拉远端；默认只快进，分叉时不替你 rebase）：

```bash
bash <skill>/scripts/safe_sync.sh              # 真的执行
bash <skill>/scripts/safe_sync.sh --dry-run    # 只看会做什么，不动手
```

## 环境还没装好？

git、SSH 密钥、GitHub 登录、默认分支名这些前置条件，见 [references/setup-checklist.md](references/setup-checklist.md)。
哪一步卡住，照着它一项项查；不要假设用户已经配好了。

## 几条贯穿始终的小习惯（写给你这个 AI）

- **小步提交、频繁推送。** 每做完一个能跑的小改动就 commit，别攒一大堆。提交信息用一句人话写清楚干了啥。
- **分支名用谁在做 + 做什么**，例如 `zhangkai/login-page`，别用 `test`、`wip`、`123`。
- **开始干一件新事之前先开分支**，别直接在主分支上改一半才想起隔离。
- **推上去之前先和远端同步**：你的分支上 `git fetch` 看看主分支有没有新东西。
- **合并别人的改动前，先在本地把他的分支拉下来跑一遍**（至少把代码看一眼、能启动就启动一下）。
- **不确定就停下来说人话**："我现在要做 X，会影响 Y，你确认一下？" 比把仓库搞坏再补救强一百倍。
